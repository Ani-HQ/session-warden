---
name: Bug report
about: Something misbehaved — a wrong rotation, a missed stall, a broken module
labels: bug
---

**What happened**

**What you expected**

**Environment**
- OS / distro:
- Agent runtime and version (OpenClaw / Hermes / other):
- session-warden version (`session-warden version`):

**Doctor output**

```
# paste the output of:
session-warden doctor
```

**Relevant log lines**

```
# e.g. from state/scan.log, state/reap.log, or the module's log under state/
```

_Never paste `config/thresholds.env` or anything from `~/.config/session-warden/secrets.env` — redact tokens and chat IDs._
