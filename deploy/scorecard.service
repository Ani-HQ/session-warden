[Unit]
Description=session-warden model scorecard (weekly Hermes A/B benchmark)
After=network-online.target

[Service]
Type=oneshot
ExecStart=%h/session-warden/bin/scorecard.sh
# Inherit the same user environment the warden cron uses (PATH, DBUS, etc.)
Environment=HOME=%h
