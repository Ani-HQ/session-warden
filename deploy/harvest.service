[Unit]
Description=session-warden skill harvester (weekly skill mining)
After=network-online.target

[Service]
Type=oneshot
ExecStart=%h/session-warden/bin/harvest-skills.sh
# Inherit the same user environment the warden cron uses (PATH, DBUS, etc.)
Environment=HOME=%h
