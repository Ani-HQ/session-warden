[Unit]
Description=session-warden GBrain dream-cycle (nightly enrichment)
After=network-online.target

[Service]
Type=oneshot
ExecStart=%h/session-warden/bin/dream-cycle.sh
# Inherit the same user environment the warden cron uses (PATH, DBUS, etc.)
Environment=HOME=%h
